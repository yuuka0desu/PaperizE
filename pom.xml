<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.paperize</groupId>
  <artifactId>paperize</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>PaperizE</name>
  <description>PaperizE —— ProjectE（等价交换）内容项目（独立工程；依赖 CEPlus 引擎作为加载引擎）</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler.release>25</maven.compiler.release>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>26.1.2.build.74-stable</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
      <version>2.13.2</version>
      <scope>provided</scope>
    </dependency>
    <!-- CEPlus 引擎（本地仓库；ProjectE 作为内容项目依赖引擎 API） -->
    <dependency>
      <groupId>com.ceplus</groupId>
      <artifactId>ceplus-engine</artifactId>
      <version>0.1.0-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>
    <!-- CraftEngine（运行时 join-classpath；用于物品注册校验 API） -->
    <dependency>
      <groupId>net.momirealms</groupId>
      <artifactId>craftengine</artifactId>
      <version>26.8.2</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>PaperizE-${project.version}</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
    </plugins>
  </build>
</project>
